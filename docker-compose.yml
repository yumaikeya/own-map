version: "3.8"

services:
  node:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ./:/usr/src/app
    ports:
      - 3000:3000
    command: sh -c "yarn && PORT=3000 yarn start"
    tty: true